# CivicSign backend — runtime dependencies (only what the code imports).
# Full pre-prune snapshot kept at requirements.full.txt.bak.

# Web framework + server
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
pydantic==2.13.4
pydantic-core==2.46.4
email-validator==2.3.0      # required by pydantic EmailStr
python-multipart==0.0.29

# MongoDB (Atlas SRV needs dnspython)
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0

# Auth / security
PyJWT==2.13.0
bcrypt==4.1.3
passlib==1.7.4

# Config, rate limiting
python-dotenv==1.2.2
slowapi==0.1.9

# Integrations
stripe==14.4.1
resend==2.30.1

# PDF processing
PyMuPDF==1.27.2.3
